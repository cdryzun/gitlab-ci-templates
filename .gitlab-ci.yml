# GitLab CI for this template repository itself (not for end users)
# Enable when hosting on GitLab to validate template changes on push

# include:
#   - local: "vars/default-vars.stable.gitlab-ci.yml"
#   - local: "utils/rules.stable.gitlab-ci.yml"
